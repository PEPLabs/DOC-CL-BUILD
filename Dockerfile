#This is an example of a Dockerfile. A Dockerfile always starts with a 'FROM' declaration.
#TODO: change this Dockerfile so that it starts from the 'alpine' image rather than the 'scratch' image.
FROM scratch